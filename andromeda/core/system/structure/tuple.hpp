#pragma once


namespace Andromeda::System::Structure {
    template<typename Element>
    struct Duo {
        Element α, β;
    };

    template<typename Element>
    struct Trio {
        Element α, β, γ;
    };

    template<typename Element>
    struct Quartet {
        Element α, β, γ, δ;
    };

    template<typename Element>
    struct Quintet {
        Element α, β, γ, δ, ε;
    };

} /* Andromeda::System::Structure */